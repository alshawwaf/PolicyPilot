"""PolicyPilot — agentic Check Point access automation: turn a plain-language access request into the
correct, first-match-safe policy change, applied to a real management server or pushed straight to the
gateway as a dynamic layer, and drivable by an agent over MCP."""

__version__ = "1.0.0"
